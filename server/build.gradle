apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':gson-2.6.2')
    compile project(':shared')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
