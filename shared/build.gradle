apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('src/main/java/sqlite-jdbc-3.7.2.jar')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
